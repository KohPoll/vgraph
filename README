对统计数据的可视化。

目前主要是对字频、词频。

环境要求：php+apache，建议使用一体式的AppServ。

1.cd到apache服务器根目录(httpd)，若是AppServ，则cd到www目录；
2.git clone git://github.com/KohPoll/vgraph.git;
3.浏览器访问http://localhost/vgraph/index.html(若你有修改端口号，请进行相应修改);
